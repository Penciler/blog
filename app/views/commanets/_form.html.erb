<%= form_for(commanet) do |f| %>
  <% if commanet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(commanet.errors.count, "error") %> prohibited this commanet from being saved:</h2>

      <ul>
      <% commanet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %>
    <%= f.text_field :post_id %>
  </div>

  <div class="field">
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
